# GuitarApp Curriculum Review — verified (2026-08-12, hop H2)

Re-run of the prior curriculum review (review-curriculum.mjs, report.json) PLUS an
audit of the actual SHIPPING curriculum. Heidi doctrine: verify on disk, do not trust
prior self-reports. Every claim below was re-executed this session.

## What was INDEPENDENTLY VERIFIED (green)

1. PRACTICE ENGINE TESTS — `node --test 06-prototypes/practice-engine/practice-engine.test.mjs`
   => 17 pass / 0 fail. (Prior report claimed 17; RE-CONFIRMED.)
2. ARITHMETIC CHORD GATE (AGENTS.md rule 8 ship gate)
   `node 06-prototypes/step0/run-chord-check.js` =>
   23 lessons | 71 chords checked | 0 errors | 0 warnings. CHORDS-VERIFIED-OK.
   NOTE: this gate scans 07-app/content/lessons (the SHIPPING set), per commit 41a54f8.
3. PRACTICE-LESSON SPINE MATCH — `validate-practice-lessons.mjs` => 20 practice lessons,
   0 failures, ALL MATCH the teaching spine (§5.1).
4. MOAT INTEGRITY — per-pair practice set in 07-app/content/practice/ = exactly 28 files,
   distinct unordered pairs = 28, missing = 0, extra = 0. 28 == C(8,2) for the canon
   8-chord set (easyC collapses to C). The weak-pair review surface is COMPLETE.
5. §5.2 "many options" — all 20 practice lessons in 05-content/practice carry an
   11-drill `practice_menu` (>=3 each). Requirement SATISFIED.
6. C-IDENTITY-SPLIT (root-cause fix) — chord-canon.js collapses easyC->C; PracticeStore
   merges them into ONE fluency slot. Re-verified: canonPairKey(easyC,Em)==canonPairKey(C,Em)
   and getSkillMap has a single 'C' entry. Moat no longer fragments. OK.

## CRITICAL FINDING the prior review MISSED (P0 — false assurance)

The curriculum review scanned 05-content/ (20 lessons). The SHIPPING curriculum is
07-app/content/lessons/ (23 lessons, per its own manifest.json AND per the ship-gate
commit 41a54f8). The two sets DIVERGE:

  - 07-app ships 3 lessons OUTSIDE the locked 20-group spine (§5.0):
      L21 "Holding the Pick"       type=technique  chords=[]          (extra technique lesson)
      L22 "Switching Between Em and C"  type=technique chords=[Em,easyC]  (transition drill)
      L23 "Your First 3-Chord Song"     type=technique chords=[G,Em,easyC] (a SONG as a technique lesson)
  - 05-content/ has NO L21-23 at all.
  - The locked-spine doc (CURRICULUM-AND-PRACTICE-STRUCTURE.md §5.0/§5.1) NEVER mentions
    L21/L22/L23 — grep returns zero hits for "holding the pick", "first 3-chord", "L21/22/23".

CONSEQUENCE: the prior review's headline "20 lessons, all match the locked spine" is
technically true for 05-content but MISLEADING, because the app the user actually installs
ships 23 lessons, 3 of which the documented structure neither accounts for nor permits.
The "20 groups, locked" invariant in §5.0 is violated on disk by the shipping build.

## CONFIRMED LOW findings (consistent with prior review)

A. SONGS-NOT-SEPARATED — §5.4/§5.6 declares songs a SEPARATE category (removed from the
   technique spine), but on disk:
     - L03 still embeds "Your first song — Em to easy C" (verified in both 05-content and 07-app).
     - L23 IS a song presented as a technique lesson.
     - No SONGS category directory exists under 05-content/ or 07-app/content/.
   The directive is documented as resolved but NOT executed on disk.

B. NO-C-GRADUATION-LESSON — easyC (L3) -> standard C (L8) with no explicit teaching beat
   naming the swap. Memory continuity is already solved by canonChord(easyC)==C (verified),
   so this is a beginner-clarity nicety, NOT a moat bug. Non-blocking.

## RECOMMENDATION (needs Heidi decision — structural, not auto-fixable)

The 20-group spine is the documented "locked" structure, but the shipping app already
ships 23 lessons. Pick ONE source of truth:

  Option 1 (reconcile doc up): acknowledge L21 (pick grip) as a pre-L02 technique slot,
    fold L22/L23 into the SONGS category (§5.6) as S2/S3, and update §5.0/§5.1 to say
    "20 technique groups + N technique-support lessons + SONGS category." Keep 1:1 practice
    for the 20 groups; songs get their own practice layer.
  Option 2 (reconcile build down): remove L21-23 from the shipping manifest until the
    structure decision lands, so the build matches the locked doc.

Either way, the duplication (05-content/ 20 vs 07-app/content/lessons 23) must be resolved
so the reviewed set == the shipped set. Recommend Option 1 — the 3 extra lessons are
genuinely useful (pick grip, Em/C transition, first song performance) and match the
"first song at L3 / earliest of all competitors" advantage the doc celebrates.

## Files
- This review: 06-prototypes/curriculum-review/REVIEW-2026-08-12-v2.md
- Audit script (shipping set): 06-prototypes/curriculum-review/audit-shipping.mjs
- Prior machine report (20-lesson only): 06-prototypes/curriculum-review/report.json
