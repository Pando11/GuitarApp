# GuitarApp — Tickets to finish together (generated 2026-08-30)

**Status when you left:** Waves 1, 2, 3, 4 built and VERIFIED on disk (parent re-ran every self-test; chord gate still GREEN 25/81/0/0). You asked me to build as much as possible and leave tickets for the parts that need YOU (HITL) or are genuinely blocked (no GPU / no PocketBase binary / missing source assets).

Everything below is real, not fabricated. Each ticket says what's done, what's blocked, and the exact next action.

──────────────────────────────────────────
## TICKET H1 — Wave 0 de-risk vertical slice (HITL)
**What's built (agent-side):** Emerald Hollow assets exist; Sage coaching bridge (sageCoach) + Story Memory (storyMemory) + backup buttons (BI-8) all done and Rule-5-clean. The building blocks for the slice are on disk.
**What needs YOU:** The actual 5–10 real-beginner test on their own phones. Score the 3 yes/no's:
  1. Can they play the chord AFTER? (real learning)
  2. Did they smile / want to keep going? (fun)
  3. Did Sage HEAR them correctly on their own phone? (load-bearing risk — listening engine)
**Blocker type:** HITL. Agent cannot run a human test.
**Next action:** Heidi runs the slice on 5–10 beginners. If the listener fails on real phones → STOP and fix the listener before anything else (cheapest place to learn it).

──────────────────────────────────────────
## TICKET H2 — PocketBase live sync (BLOCKED: no binary)
**What's built:** `07-app/core/pocketbaseSync.js` — full Layer-3 client crypto (PBKDF2-SHA256 150k iters + AES-256-GCM), recovery-phrase generation, push/pull record shape. Offline crypto roundtrip test: **12/12 PASS** (verified in node v22 webcrypto).
`07-app/core/pocketbaseSchema.json` — collection schema (student_id, ciphertext, iv, updated_at).
**What's blocked:** The live server round-trip (actual fetch POST/GET to PocketBase) — no `pocketbase` binary on this machine, no server on 127.0.0.1:8090. All fetch() calls are commented out; module only exercises pure crypto.
**Next action (when you have a machine/instance with PocketBase):** stand up the PB binary, create the collection from `pocketbaseSchema.json`, uncomment the fetch calls in pocketbaseSync.js, run a push/pull against the real server.
**Decision made for you:** client-side crypto handshake is done and verified; server is the only gap.

──────────────────────────────────────────
## TICKET H3 — Jam generative audio (BLOCKED: no GPU/RunPod)
**What's built:** `07-app/core/jamSession.js` — PART A on-device fully done + tested (**16/16 PASS**): `gradeStudentPhrase()` grades student chords vs known set; `emitFacts()` emits facts-only payload (NO audio leaves device, AMENDMENT-05 holds).
**What's blocked:** PART B generative response (ACE-Step music / YuE vocal/instrumental round-trip) — needs a cloud GPU on RunPod; none available this session. `generateResponse()` is a stub that throws `GEN_BLOCKED` with a clear marker.
**Next action:** when RunPod pod is up (the EU-RO-1 World-1 pod is currently GPU-supply-blocked per memory; a different region/instance needed), wire `generateResponse()` to the ACE-Step/YuE endpoint and re-run PART B test.

──────────────────────────────────────────
## TICKET H4 — L01-open-c lesson asset (BLOCKED: missing source)
**What's flagged:** `07-app/godot/data/lesson_manifest.json` references `res://assets/lessons/L01-open-c.mp4` + `.ogg` which DO NOT exist — `assets/lessons/` is empty. The manifest now carries:
  `"_todo_blocked": "TODO(BLOCKED): res://assets/lessons/L01-open-c.mp4 not generated yet - assets/lessons/ is empty (do not fabricate the file)"`
**What's needed:** generate the L01 open-chord lesson video + voice (FLUX/Wan/Chatterbox pipeline, or a placeholder) and drop into `07-app/assets/lessons/`.
**Next action:** add L01 asset generation to the world-factory pipeline queue; remove the `_todo_blocked` flag once the file lands.

──────────────────────────────────────────
## TICKET H5 — 5 orphan shipping lessons (decision needed)
**Audit finding (Wave 1, verified):** 20 authoring lessons (05-content/) all map to shipping, BUT 5 shipping lessons in `07-app/content/lessons/` have NO authoring source:
  - holding-the-pick
  - switching-em-and-c
  - first-three-chord-song
  - new-chord-f
  - new-chord-a7
**Decision options (pick one):** (a) backfill authoring source for each in `05-content/`; (b) demote/remove them from the 25-lesson shipping set (would drop count to 20); (c) keep as script-only and generate authoring later.
**Next action:** Heidi decides a/b/c; if (a), I can write the authoring JSON from the existing shipping lesson data.

──────────────────────────────────────────
## TICKET H6 — Spec song-count wording 10→11 (quick edit)
**What's pinned (file:line):** stale "10 songs" wording at:
  AGENTS.md:42 · 02-spec/guitar-app-spec-AMENDMENT-12.md:49 · AMENDMENT-13.md:44 & :53 · AMENDMENT-17.md:158 & :164 & :307 · AMENDMENT-MYSTERY-MODE.md:10 & :20 · 06-prototypes/song-discovery-q5-prototype.html:280
**Facts:** catalog = 11 songs (SP01–SP11). SP06 House of the Rising Sun + SP11 Amazing Grace are public domain (progressions.json `public_domain:true`).
**Next action:** Heidi approves the wording change; I sweep those 10 locations to "11" (or we leave prototype HTML alone if it's throwaway).

──────────────────────────────────────────
## TICKET H7 — PWA live-browser smoke (HITL)
**What's built + verified offline:** `07-app/app.js` hardened (SW registration, `?dogfood=1` tolerant, file:// safe — node --check PASS), `service-worker.js` bumped to guitarapp-v2 + precache fixed, `manifest.webmanifest` valid JSON, `backupButtons.js` BI-8 built + self-test PASS.
**What's pending:** a real browser/service-worker smoke (renderer, tuner, lesson player actually rendering) — cannot be done headlessly; needs a browser/device.
**Next action:** Heidi opens the PWA in a browser (localhost + `?dogfood=1`) and confirms boot + SW cache + a lesson renders.

──────────────────────────────────────────
## TICKET H8 — Wave 5 integration + human lyric read-through (HITL)
**What's buildable:** all 5 ship-gate sweeps are re-runnable now (chord gate already GREEN). Counsel-prep pack can be assembled from the disclaimers/PD flags already in progressions.json.
**What's HITL:** real-device mic dogfood (install PWA on phone, verify listening + voice-first) and the HUMAN lyric read-through — a regex can't catch a paraphrased lyric (AMENDMENT-14 CRITICAL).
**Next action:** Heidi (or a human) reads every song's hint/reveal text for paraphrased-lyric leaks, signs off; then I assemble the counsel-prep pack.

──────────────────────────────────────────
## SUMMARY FOR HEIDI
- ✅ Built & verified this session: Waves 1, 2, 3, 4 (Godot landing, Sage bridge, Story Memory, backup buttons, PocketBase crypto, entitlement stub, all 5 AI features' on-device logic, prototype rebuild, promotion audit).
- ⏳ Needs YOU (HITL): H1 (Wave 0 beginner test), H7 (browser smoke), H8 (dogfood + lyric read-through), H5 decision (orphan lessons), H6 approval (song wording).
- 🚫 Blocked by environment (flagged, not faked): H2 (PocketBase server binary), H3 (jam GPU/RunPod), H4 (L01 asset generation).

All new code is committed (see git log). Verification scripts left at:
  `02-spec/_verify-post-agents.sh` (Wave 1–3 batch)
  `02-spec/_verify-V2.sh` (Wave 2/4 batch)
Re-run either to re-prove disk state.
